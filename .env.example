# Reqquli Environment Configuration
# Copy this file to .env and update values

# ============================================
# SECURITY (REQUIRED - NO DEFAULTS IN PRODUCTION)
# ============================================
# CRITICAL: Generate secure secret with: openssl rand -base64 32
# Minimum 32 characters required. Application will NOT start without this.
JWT_SECRET=CHANGE_ME_USE_OPENSSL_RAND_BASE64_32_OUTPUT

# ============================================
# DATABASE
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=reqquli_admin
DB_PASSWORD=reqquli_dev
DB_NAME=reqquli_db

# Production Database SSL Configuration
# DB_CA_CERT=-----BEGIN CERTIFICATE-----...
# DB_ALLOW_SELF_SIGNED=false  # Set to 'true' ONLY for Azure managed databases

# ============================================
# APPLICATION
# ============================================
NODE_ENV=development
PORT=3000
CLIENT_URL=http://localhost:5173

# Production Cookie Configuration
# COOKIE_DOMAIN=.yourdomain.com  # Domain for auth cookies